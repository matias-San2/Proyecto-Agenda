<!DOCTYPE html>
<html lang="<%= (locals.personalization && personalization['locale.language']) || 'es' %>">
<head>
  <meta charset="UTF-8">
  <title>Mi Perfil - Personalización</title>
  <link rel="stylesheet" href="/css/navbar.css">
  <link rel="stylesheet" href="/css/perfil.css">
  <link rel="stylesheet" href="/css/footer.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  
  <!-- PASO 2 APLICADO: El bloque <style> se reemplaza por esta única línea -->
  <%- include('partials/personalization-styles') %>

</head>
<body data-theme="<%= (locals.personalization && personalization['theme.mode']) || 'light' %>">
  <div class="container-fluid">
    <div class="row">
      <%- include('navbar') %>

      <div class="main">
        <div class="top-header">
          <h1 class="page-title">Mi Perfil</h1>
          <p class="page-subtitle">Personaliza la apariencia de tu interfaz</p>
        </div>

        <div class="row justify-content-center">
          <div class="col-12 col-md-8 col-lg-6">
            <div class="profile-card">
              
              <!-- Información del usuario -->
              <div class="user-info">
                <div class="user-avatar">
                  <i class="fas fa-user-circle"></i>
                </div>
                <div class="user-details">
                  <h3><%= user.nombre %></h3>
                  <p><%= user.email %></p>
                </div>
              </div>

              <!-- Formulario de personalización (sin cambios) -->
              <form id="personalizationForm">
                <div class="form-section">
                  <h4><i class="fas fa-palette"></i> Tema</h4>
                  <!-- Modo de tema -->
                  <div class="form-group">
                    <label>Modo:</label>
                    <div class="radio-group">
                      <input type="radio" id="light-mode" name="theme.mode" value="light" 
                             <%= (!personalization['theme.mode'] || personalization['theme.mode'] === 'light') ? 'checked' : '' %>>
                      <label class="radio-option" for="light-mode">
                        <i class="fas fa-sun"></i> Claro
                      </label>
                      <input type="radio" id="dark-mode" name="theme.mode" value="dark"
                             <%= personalization['theme.mode'] === 'dark' ? 'checked' : '' %>>
                      <label class="radio-option" for="dark-mode">
                        <i class="fas fa-moon"></i> Oscuro
                      </label>
                    </div>
                  </div>
                  <!-- Color principal -->
                  <div class="form-group">
                    <label>Color principal:</label>
                    <div class="color-group">
                      <% const colors = [
                        { value: '#1a3c7c', name: 'Azul' },
                        { value: '#d53232ff', name: 'Rojo' },
                        { value: '#059669', name: 'Verde' },
                        { value: '#7c3aed', name: 'Morado' },
                        { value: '#ea580c', name: 'Naranja' }
                      ]; %>
                      <% colors.forEach(color => { %>
                        <div class="color-option-wrapper">
                          <input type="radio" 
                                 id="color-<%= color.value.replace('#', '') %>" 
                                 name="theme.primary_color" 
                                 value="<%= color.value %>" 
                                 <%= (!personalization['theme.primary_color'] && color.value === '#1a3c7c') || 
                                     personalization['theme.primary_color'] === color.value ? 'checked' : '' %>>
                          <label for="color-<%= color.value.replace('#', '') %>" class="color-option">
                            <span class="color-preview" style="background-color: <%= color.value %>"></span>
                            <span class="color-name"><%= color.name %></span>
                          </label>
                        </div>
                      <% }); %>
                    </div>
                  </div>
                </div>

                <!-- Selector de escala de fuente -->
                <div class="form-section">
                  <h4><i class="fas fa-text-height"></i> Tamaño de fuente</h4>
                  <div class="form-group">
                    <label>Escala de texto:</label>
                    <div class="radio-group">
                      <% 
                        const fontScales = [
                          { value: 'pequeno', label: 'Pequeño', icon: 'fa-font' },
                          { value: 'mediano', label: 'Mediano', icon: 'fa-font' },
                          { value: 'grande', label: 'Grande', icon: 'fa-font' }
                        ];
                        const currentScale = (personalization && personalization['font.scale']) || 'mediano';
                      %>
                      <% fontScales.forEach(scale => { %>
                        <input type="radio" 
                               id="font-<%= scale.value %>" 
                               name="font.scale" 
                               value="<%= scale.value %>"
                               <%= currentScale === scale.value ? 'checked' : '' %>>
                        <label class="radio-option" for="font-<%= scale.value %>">
                          <i class="fas <%= scale.icon %>" style="font-size: <%= scale.value === 'pequeno' ? '0.8em' : (scale.value === 'grande' ? '1.2em' : '1em') %>;"></i> 
                          <%= scale.label %>
                        </label>
                      <% }); %>
                    </div>
                  </div>
                </div>

                <div class="form-section">
                  <h4><i class="fas fa-globe"></i> Idioma</h4>
                  <div class="form-group">
                    <label>Idioma de la interfaz:</label>
                    <select name="locale.language" class="form-select">
                      <option value="es" <%= (!personalization['locale.language'] || personalization['locale.language'] === 'es') ? 'selected' : '' %>>
                        🇪🇸 Español
                      </option>
                      <option value="en" <%= personalization['locale.language'] === 'en' ? 'selected' : '' %>>
                        🇺🇸 English
                      </option>
                    </select>
                  </div>
                </div>

                <!-- Botones de acción -->
                <div class="form-actions">
                  <button type="button" id="previewBtn" class="btn btn-outline">
                    <i class="fas fa-eye"></i> Vista previa
                  </button>
                  <button type="submit" id="saveBtn" class="btn btn-primary">
                    <i class="fas fa-save"></i> Guardar cambios
                  </button>
                </div>

                <!-- Loader y mensajes -->
                <div id="loader" class="loader" style="display: none;">
                  <i class="fas fa-spinner fa-spin"></i> Guardando...
                </div>
                <div id="message" class="message"></div>
              </form>
            </div>
          </div>
        </div>
      </div>

      <%- include('footer') %>
    </div>
  </div>

<script>
    // Evento principal - Submit del formulario (guardar cambios)
    document.getElementById('personalizationForm').addEventListener('submit', async function(e) {
      e.preventDefault();
      const loader = document.getElementById('loader');
      const saveBtn = document.getElementById('saveBtn');
      const messageDiv = document.getElementById('message');

      // Mostrar loader y deshabilitar botón
      loader.style.display = 'block';
      saveBtn.disabled = true;
      messageDiv.style.display = 'none';

      try {
        const formData = new FormData(this);
        const parameters = Object.fromEntries(formData.entries());
        
        const response = await fetch('/api/personalization', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ parameters })
        });

        const result = await response.json();
        if (!result.ok) {
          throw new Error(result.error || 'Error al guardar los cambios.');
        }

        // Mostrar mensaje de éxito y recargar la página para aplicar cambios
        messageDiv.innerHTML = `<i class="fas fa-check-circle"></i> Cambios guardados. La página se recargará...`;
        messageDiv.className = 'message success';
        messageDiv.style.display = 'block';

        setTimeout(() => {
          window.location.reload();
        }, 1500); // Espera para que el usuario vea el mensaje

      } catch (error) {
        messageDiv.innerHTML = `<i class="fas fa-exclamation-triangle"></i> Error: ${error.message}`;
        messageDiv.className = 'message error';
        messageDiv.style.display = 'block';
        loader.style.display = 'none';
        saveBtn.disabled = false;
      }
    });

    // El botón de vista previa ya no es necesario con este flujo, pero lo dejamos por si se reutiliza.
    // Puedes ocultarlo o eliminarlo del HTML si lo deseas.
    document.getElementById('previewBtn').addEventListener('click', function() {
        const messageDiv = document.getElementById('message');
        messageDiv.innerHTML = `<i class="fas fa-info-circle"></i> La vista previa está desactivada. Guarda los cambios para aplicarlos.`;
        messageDiv.className = 'message info';
        messageDiv.style.display = 'block';
        setTimeout(() => { messageDiv.style.display = 'none'; }, 4000);
    });
</script>
</body>
</html>